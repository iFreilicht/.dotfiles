{
    "nix.serverSettings": {
        "nixd": {
            "formatting": {
                "command": [
                    "${env:HOME}/.nix-profile/bin/nixfmt"
                ],
            }
        }
    }
}